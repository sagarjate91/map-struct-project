spring:
  application:
    name: my-application

  datasource:
    url: jdbc:mysql://localhost:3306/mapstruct
    username: root
    password: admin
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
   